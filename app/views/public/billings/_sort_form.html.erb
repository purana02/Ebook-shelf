<%= form_with url: customer_billings_path, method: :get, local: true do |f| %>
  <form>
    <div class="form-group row">
      <%= f.select :site_id, options_from_collection_for_select(Site.all, :id, :name), { include_blank: "--選択してください--" } %>
      <%= f.submit "並べ替え", class:"btn btn-sm btn-outline-primary" %>
    </div>
  </form>
  <% end %>
