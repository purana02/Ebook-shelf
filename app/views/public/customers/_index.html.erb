<table class='table table-hover table-inverse'>
  <thead>
    <tr>
      <th>登録日</th>
      <th>タイトル</th>
      <th>購入したサイト名</th>
    </tr>
  </thead>
  <tbody>
    <% havings.each do |having| %>
    <tr>
      <td><%= having.created_at.strftime('%Y/%m/%d') %></td>
      <td>
        <%= link_to comic_path(having.comic_id) do %>
          <%= having.comic.title %>
        <% end %>
      </td>
      <td><%= having.site.name %></td>
    </tr>
    <% end %>
  </tbody>
</table>