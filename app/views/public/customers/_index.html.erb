<table class='table table-hover table-inverse'>
  <thead>
    <tr>
      <th>登録日</th>
      <th>タイトル</th>
      <th>購入したサイト名</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% comics.each do |comic| %>
    <tr>
      <td><%= comic.created_at.strftime('%Y/%m/%d') %></td>
      <td><%= comic.title %></td>
      <td><%= comic.site.name %></td>
    </tr>
    <% end %>
  </tbody>
</table>